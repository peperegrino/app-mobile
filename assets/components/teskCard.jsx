import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';

export default function taskCard ({item, onDelete, onEdit}) {
    const priorityColor = TouchableNativeFeedbackComponent.colors.priority
    [item.priority] || TouchableNativeFeedbackComponent.colors.textSub;

    
}